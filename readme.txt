# PHP ToDo App with MySQL

## Setup

1. Import `sql/schema.sql` into your MySQL.
2. Update `config/db.php` with DB credentials.
3. Access `index.php` in browser.

## Git Tracking

- Track `.php` files normally.
- Use `sql/schema.sql` to track DB schema.
- Use Git branches to track feature or schema changes.
